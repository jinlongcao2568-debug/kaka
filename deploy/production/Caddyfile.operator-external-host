{
	admin off
	servers {
		strict_sni_host on
	}
}

import /etc/caddy/KakaOperatorRoutes

{$KAKA_OPERATOR_HOSTNAME} {
	tls internal

	import kaka_operator_routes
}

http://{$KAKA_OPERATOR_HOSTNAME} {
	import kaka_operator_routes
}

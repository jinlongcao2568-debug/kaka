结论：需修正任务包

判断：
- active subpacket 基本信息：`control/current_task.yaml` 当前 active 包声明为 `packet_kind=EXECUTABLE_SCOPED_SUBPACKET`，`source_blueprint_batch_id=PKT-P0-08-formal-route-map-split`，`packet_id=subpacket_id=PKT-P0-08-S6-control-foundation-full-fix`，`status=ACTIVE`，`change_class=MANDATORY_HUMAN_REVIEW`。
- 合规点：`source_blueprint_batch_id` 来自 `control/task_packet_library.yaml` 已注册 blueprint；`task_packet_library` 也明确自己是 backlog/blueprint 库，正式 active source 是 `control/current_task.yaml`。`repo_status.md`、`milestone_status.yaml`、开工裁决页三处状态一致，当前都允许继续这个治理包，但不允许自动切到业务包。`declared_changed_paths` 全部落在 `allowed_modification_paths` 内，未命中 `forbidden_modification_paths`。`change_class` 与触达路径匹配，`human_review_required=true` 且 owner review 列表覆盖了自动化/架构/治理/测试/release/model governance。`required_scripts` 包含 `check-automation-readiness.ps1` 和 `check-release.ps1`。`stop_conditions` 已声明，且仓库规则也要求执行后停机汇报。
- 风险点：`declared_changed_paths` 一共 11 个，超过 `automation_task_packet_rules.yaml` 和《自动化开发动作门禁表》冻结的 `<=10` 上限；这是硬违规。docs 2 个、control 3 个都没超，但总文件数超了。这个包语义上还是单主题“control foundation full fix”，但实际横跨 docs/control/scripts/contracts/tests 五层，已经偏大。另一个次级风险是：同名 `packet_id/subpacket_id` 不在 `task_packet_library.yaml` 的 `packets:` 列表里，当前追溯主要依赖 `source_blueprint_batch_id + backlog_packet_ref`，不是最强追溯形态。还有一处清洁度问题：`required_scripts` 里 `check-automation-readiness.ps1` 重复写了 2 次。
- 是否越界：未越界到 forbidden path；未触及 `src/**`、`handoff/**`、`contracts/governance/**`、`contracts/release/**`。但确实触及了 `scripts/**`、`tests/**`、`contracts/testing/**`、`control/**`，属于高风险受控面，不能按 direct batch 对待。
- 是否漏关键脚本：不漏。`check-automation-readiness.ps1`、`check-release.ps1` 都在；前者重复一次，但不是缺失。
- 是否符合“路线图指路、task_packet_library 给候选、current_task 唯一 active、scoped subpacket 单次执行、repo_status/milestone_status 看状态”这套方法：大框架是符合的。路线图仍只是导航，`task_packet_library` 仍是候选/blueprint 库，`current_task` 仍是唯一 active source，`repo_status`/`milestone_status` 仍是状态真源。真正不符合的是这个 active 包的粒度，不能算“长期稳定”的合规 scoped subpacket，因为它超了 `<=10` 文件硬门槛。
- 当前如果进入实现，必须遵守的边界：只能在当前白名单文件里动；不得改 `docs/L0.md`、`docs/裁决总表.md`、`docs/D1_研发_Codex执行手册.md`、`docs/AX9S_开发执行路由图.md`、`control/task_packet_library.yaml`、`control/milestone_status.yaml`、`control/review_gate_matrix.yaml`、`control/automation_*`、`src/**`、`handoff/**`；不得放宽 external release、Stage 8 real execution、Stage 9 real payment/delivery；跑完 required scripts 后必须停机汇报，不能自动进入下一包。

给总控窗口的建议：
- 下一步应做什么：先修 task packet 粒度，再决定是否进入实现审查。当前 repo 状态本身允许做这个治理包，但 active packet 设计还不够稳。
- 如果不能继续，最小修正路径是什么：把 `declared_changed_paths` 收到 `<=10`。最省事的做法是把 1 个非必须文件移出当前包；如果 11 个都必须改，就拆成两个 scoped subpacket，其中一个只做 control/scripts 侧，另一个再承接 contracts/tests 或文档同步。修正后同步更新 `allowed_modification_paths`、`impacted_assets`、`required_scripts`、`stop_conditions`，然后重新审一次 active 包；不要自创新主线，不要自动进入下一包。

附注：本步只做只读审查，未改文件，未运行正式校验脚本。
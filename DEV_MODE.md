# DEV_MODE

本文件定义仓库的开发友好模式。它的目标是防止治理规则压住普通功能开发。

## 1. 模式定义

### DEV_MODE

用于：

- 本地功能开发
- UI / API / worker / repository 实现
- mock、fixture、假数据
- 普通单元测试和局部回归
- 文档小修和一致性同步

默认行为：

- 可以直接改代码、测试、脚本、文档和本地机器资产
- 不要求 task packet
- 不要求全量读 D1-D14
- 不要求 final gate
- 只跑相关测试

### TEST_MODE

用于：

- sandbox
- dry-run
- regression
- controlled sample
- 明确授权 pilot

默认行为：

- 可以测试触达、支付、交付、退款、自动退款等能力
- 必须标记测试态
- 不得把测试成功写成生产 live 成功
- 涉及外部目标时要记录目标、输入、输出和审计线索

### PROD_LIVE_MODE

用于：

- 真实用户或真实客户状态
- 真实第三方生产系统
- 真实发送、扣款、交付、退款
- 对外 release 或客户可见承诺

默认行为：

- 必须有具体目标授权
- 必须有审批、审计和 operator action
- 支付、交付、退款必须有对账、回滚或暂停机制
- 必须按受控窗口或 task packet 处理

## 2. 判定规则

优先按“是否影响真实外部状态”判定，而不是按关键词判定。

| 关键词 | DEV_MODE / TEST_MODE | PROD_LIVE_MODE |
|---|---|---|
| live | 可以写模拟、配置、readback、dry-run、pilot 准备 | 真实外部执行才需要生产门禁 |
| 支付 | 可以写 sandbox 支付、假回调、对账测试 | 真实扣款需要授权和审计 |
| 交付 | 可以写 mock 交付、候选包、下载审计测试 | 客户真实下载/交付需要门禁 |
| 触达 | 可以写 outbox、模板、dry-run、sandbox send | 真实发送需要审批和 operator action |
| 退款 | 可以写 sandbox/dry-run/受控试点自动退款 | 生产退款需要授权、审批、审计、对账、回滚 |

## 3. AI 代理执行规则

AI 代理遇到普通开发请求时：

1. 默认进入 `DEV_MODE`。
2. 先实现功能，不先扩写治理文档。
3. 只读取必要文件。
4. 只运行相关测试。
5. 旧测试或旧文档如果把 DEV/TEST 能力误判成禁止，应修正旧断言。

AI 代理只有在明确进入 `PROD_LIVE_MODE` 时，才启用完整生产门禁。

## 4. 新手保护规则

为了避免项目继续变压抑：

- 不因为没看完全部文档就停止开发。
- 不因为没写 task packet 就停止普通开发。
- 不因为测试没有全量跑完就否定局部功能进展。
- 不因为存在生产风险就禁止做 mock/sandbox/dry-run。
- 不用“必须完美”作为开始写功能的前提。

fastapi==0.139.2
pydantic==2.13.4
sqlalchemy==2.0.51
alembic==1.18.5
psycopg[binary]==3.3.4
uvicorn==0.51.0
httpx==0.28.1
PyYAML==6.0.3
# Fixed SKU evidence bundle renders review-ready Chinese PDF/HTML/ZIP artifacts.
reportlab==5.0.0
# Stage4 PDF forensics imports fitz at module load; keep the wheel explicit for clean CI/test environments.
PyMuPDF==1.28.0
# Stage2/3 embedded PDF text extraction imports pypdf directly; do not rely on document-tool extras.
pypdf==6.14.2
markitdown[all]==0.1.6
# Stage1/2 parser/adaptive selector enrichment plus gated HTTP/browser transports.
# Fetcher/browser extras stay non-default and must run behind our allowlist/audit/snapshot boundary.
scrapling[fetchers]==0.4.11
# Browser worker invokes Playwright directly; keep it explicit instead of relying on Scrapling transitively.
playwright==1.61.0

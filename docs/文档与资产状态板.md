# 文档与资产状态板（Post-Repair authority convergence + B10 closeout 后）

## 1. 权威 / 状态 / 导航分层

### 1.1 正式权威 / 裁决源

- L0 上位母版：`L0.md`
- 唯一正式裁决索引面：`裁决总表.md`
- 主题正式文档：`D1-D14`

### 1.2 正式状态源

- `control/repo_status.md`
- `control/current_task.yaml`
- `control/milestone_status.yaml`
- `control/release_manifest.yaml`
- `control/model_release_manifest.yaml`
- 本状态板
- `docs/正式业务代码开发开工裁决页.md`：仅负责 internal leadops development 的 conditional-go 作用域，不是仓库总体 readiness 唯一状态源

### 1.3 执行导航源

- `docs/AX9S_开发执行路由图.md`：受控路线图导航资产；在 machine-readable 索引中继续按 candidate navigation asset 标记，以明确其不是正式状态源
- `docs/AX9S_开发执行路由图.md` 现在只承载 formal stage 1-9 current route；历史修复批与 future unlock 历史导航已分层，不再混写为当前主线
- `docs/自动开发任务包模板.md`：任务包模板，不是状态源

### 1.4 历史过程材料

- `docs/quality/*`
- `archive/*`
- `control/external_unlock_prerequisite_state.yaml` / `control/future_unlock_decision_state.yaml`：R6 决策时点快照，不是当前 repo readiness 状态源

## 2. 正式文档状态

| 资产 | 角色 | 当前状态 | 备注 |
|---|---|---|---|
| D1 | 执行手册 | DRAFT | 正式执行纪律与 change gate 来源之一 |
| D2 | 正式对象契约与字段字典 | DRAFT | |
| D3 | 正式规则码总表与双闸门说明 | DRAFT | |
| D4 | OpenAPI 接口契约 | DRAFT | |
| D5 | 页面、导出与人工复核规范 | DRAFT | |
| D6 | 字段策略与客户交付字段规范 | DRAFT | |
| D7 | 对象级交付矩阵与外发治理规范 | DRAFT | |
| D8 | 真实竞争者识别、可售对象与销售推进规范 | DRAFT | |
| D9 | 联系对象与销售触达规范 | DRAFT | |
| D10 | 订单支付交付与治理反馈规范 | DRAFT | |
| D11 | 测试验收与金标回归清单 | DRAFT | 正式回归 / release gate 来源之一 |
| D12 | 部署、发布与运行治理规范 | DRAFT | |
| D13 | 能力总表与放行边界总表 | DRAFT | 能力消费 / 放行 / 对外表达统一裁决来源 |
| D14 | AI / 模型治理规范 | DRAFT | |

## 3. 正式机器资产状态

| 资产包 | 当前状态 | 备注 |
|---|---|---|
| contracts core | EFFECTIVE | schemas / enums / rules / gates |
| governance / release | EFFECTIVE | public boundary / coverage / field policy / approval / release |
| testing | EFFECTIVE | assertions / golden / regression / release checklist |
| handoff | EFFECTIVE | stage handoff / integration matrix / examples / validation / dependency order |
| scripts | EFFECTIVE | validate / golden / governance / release / lint / doctor / check-automation-readiness / check-handoff-dependencies |
| api / ui | EFFECTIVE | Stage 7-9 internal preview / draft route + projection + repository surfaces 已通过 `src/api/main.py` / `src/api/deps.py` 接入 FastAPI transport；Stage 1-6 仍为 contract-ready / transport-not-wired |
| exceptions / sales | EFFECTIVE | D7 / D8 / D9 / D10 机器承接 |
| future unlock prerequisites | EFFECTIVE | R5 machine-readable prerequisite baseline；不等于 external unlock |
| future unlock decision | EFFECTIVE | R6 decision matrix / state；只做 strategic classification，不做 implementation approval |
| leadpack activation design prep | DRAFT / REVIEW_READY | design / implementation prep 前置设计资产；review gate / approval / audit / owner signoff 已分层；不等于 implementation approved |
| leadpack implementation decision readiness packet | DRAFT / HELD | review-only signoff/readiness packet；不等于 implementation decision |
| control | CONFIRMED | 当前 state/source authority 已收敛到单一 canonical readiness；AX9S full-repair 程序已关账为 `FULL_REPAIR_COMPLETE_REVIEW_READY`（仅程序控制态） |
| archive | EFFECTIVE | 仅历史追溯，不是现行引用面 |

补充一致性：
- `docs_layer=EFFECTIVE` 指“现行正式文档包生效”，不代表 D1-D14 每份文档 individually promoted to `EFFECTIVE`
- C 组枚举冻结：`CONFIRMED`

## 4. 当前正式状态

- 当前阶段：`PHASE_5_INTERNAL_LEADOPS_DEVELOPMENT`
- 当前判断：`READY_FOR_POST-REPAIR_MAINLINE_SELECTION`
- 当前 conditional-go：`READY_FOR_INTERNAL_LEADOPS_DEVELOPMENT`
- 当前 AX9S full-repair 程序控制状态：`FULL_REPAIR_COMPLETE_REVIEW_READY`
- 当前 formal implementation mainline：`未选定`
- 当前导航状态：`CONTROLLED_ROUTE_MAP_RESTORED`
- 当前是否 candidate-gap：`否`
- 当前是否 strategic-branch：`否`
- 当前 closure review：`已关闭`
- 当前 mainline selection：`就绪`
- 主要原因：A 项 residual drift 已收平，formal status/source authority 已统一，B10 required scripts 全部通过，AX9S full-repair 程序已整体关账；当前仓库已达到主线选择前置就绪状态，但尚未选择新的 formal implementation mainline。

作用域说明：
- `READY_FOR_POST-REPAIR_MAINLINE_SELECTION` 表示“当前仓库已完成 post-repair authority convergence，可以进入 formal mainline selection”，不表示 external 已放开，也不表示已经选定下一实现主线。
- `FULL_REPAIR_COMPLETE_REVIEW_READY` 只表示 AX9S full-repair 程序控制面已完成并可供复核；它不是 repo readiness source，也不改写当前 `READY_FOR_POST-REPAIR_MAINLINE_SELECTION`。
- `READY_FOR_INTERNAL_LEADOPS_DEVELOPMENT` 只用于内部 LeadOps 正式开发 conditional-go。
- `READY_FOR_POST-R6_CANDIDATE_GAP_BATCH` 与 `READY_FOR_POST-R6_STRATEGIC_BRANCH_BATCH` 只保留为历史 R6-path 语义，不再充当当前仓库总体 readiness。
- 历史 `M1`、`M2`、`M3`、`M4`、`M5`、`M6`、`M7`、`M8` 与 leadpack activation/readiness 相关段继续保留为历史导航，不再充当当前 formal mainline 或当前 repo readiness。
- D10 当前 formal downstream refs 已指向现存 machine-readable 资产；本轮只修正“D10 旧 machine path 仍缺失”的过期审计口径，不改 D10 正文。
- 当前仓库真实 transport 口径是：Stage 7-9 internal preview/draft surfaces 已通过 FastAPI bootstrap 接通；Stage 1-6 route registrar 继续保持 controlled unavailable / transport-not-wired。
- 当前 Stage1-2 口径是：source/route/time-range/clock/version 的 fallback 与 producer ownership 正在通过 extractor contract 收口；当前批次不改变 Stage3+ 语义。
- 当前 Stage5-6 口径是：evidence/rule gate/report/challenger 的计算正通过 evaluator/aggregator 模块从 service-local hardcode 中抽离；当前批次不改变 Stage7+ handoff 语义。
- 当前 Stage7 口径是：buyer_fit_scorecard、actor seed provenance 与 price candidate resolution 正在通过 runtime + machine-readable trace 收口；当前批次不改变 Stage8/9 handoff 语义。
- 当前 Stage8 口径是：在不新增正式对象的前提下，通过 `contact_candidate_pool` 等价实现和 vendor registry/policy resolver 收口 candidate selection 与冲突回放；当前批次不改变 Stage9 语义。
- 当前 Stage9 口径是：writeback impact executor 已从 formal defer 升级到 internal-only v0 active，只生成 additive machine-readable target mutation projection，不直接持久化 target object，也不放开任何 live payment/delivery。
- 当前 storage 口径是：本地 file-backed operator loop store 已升级为并发稳定模式，默认路径具备进程级隔离，flush 使用唯一 temp file + replace retry；这属于历史修复结果，不构成当前 formal mainline。
- 前序 readiness/signoff 批只建立 implementation decision 前 review-only packet；owner signoff、approval chain、review gate、audit refs 任一缺失仍不得进入 implementation-ready。该约束仍有效，但只属于 future unlock history，不属于当前 formal route。
- 本窗口确认仓库无 `scripts/search.ps1`；全文检索按规则 fallback 到 `rg`，且该事实已写入 B10 closeout。
- 本状态板与 `repo_status/current_task/milestone_status/release_manifest/model_release_manifest` 必须保持同一 phase/readiness。

## 5. 当前 blocked / governed 边界

- external software release：`BLOCKED`
- 线索包外发：继续要求审批链 + 审计链
- Stage 8 real execution：governed / approval-gated / blocked by default
- Stage 9 real payment / delivery / refund：governed / approval-gated / blocked by default
- model/provider/tool/source 真实外部接入：继续受 capability + review gate + governance 约束
- 高限制字段：继续按更严格规则处理
- future unlock implementation：必须基于 R6 decision + prerequisite evidence pack 另起单独 batch，不得由当前 R5/R6 资产或本批 closeout 自动触发

## 5.1 R6 decision 结果摘要

- FUTURE_UNLOCK_CANDIDATE：`leadpack_external_delivery`、`source_vendor_externalized_usage`、`external_export_surface`
- DENY_BY_DEFAULT_CONTINUE：`external_software_release`、`stage8_live_execution`、`stage9_live_payment_execution`、`stage9_live_delivery_execution`、`model_provider_externalized_usage`
- LONG_TERM_BLOCKED_OR_NEVER_DEFAULT_OPEN：`tool_provider_externalized_usage`
- 默认 post-R6 路径：`continue internal-first`

## 6. 使用规则

- 本状态板只允许存在一份正式版
- 更新时必须同步 `control/repo_status.md`、`control/current_task.yaml`、`control/milestone_status.yaml`、`control/release_manifest.yaml`、`control/model_release_manifest.yaml`、`control/reference_index.json`
- 不得用聊天、注释、口头说明替代本状态板

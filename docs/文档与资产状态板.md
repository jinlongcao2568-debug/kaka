# 文档与资产状态板（current control foundation full-fix execution window）

## 1. 权威 / 状态 / 导航分层

### 1.1 正式权威 / 裁决源

- L0 上位母版：`L0.md`
- 唯一正式裁决索引面：`裁决总表.md`
- 主题正式文档：`D1-D14`

### 1.2 正式状态源

- `control/repo_status.md`
- `control/current_task.yaml`
- `control/milestone_status.yaml`
- `control/release_manifest.yaml`
- `control/model_release_manifest.yaml`
- 本状态板
- `docs/正式业务代码开发开工裁决页.md`：仅负责 internal leadops development 的 conditional-go 作用域，不是仓库总体 readiness 唯一状态源

### 1.3 执行导航源

- `docs/AX9S_开发执行路由图.md`：受控路线图导航资产；在 machine-readable 索引中继续按 candidate navigation asset 标记，以明确其不是正式状态源
- `docs/AX9S_开发执行路由图.md` 现在只承载 formal stage 1-9 current route；历史修复批与 future unlock 历史导航已分层，不再混写为当前主线
- `docs/任务契约_Stage1-6文件分析闭环与经验库能力落地.md`：当前 Stage1-6 文件分析闭环与经验库能力落地的防漂移任务契约；约束开发顺序、能力矩阵和 Stage4-5 规则化边界，不是 phase/readiness 状态源
- `control/stage16_file_analysis_task_contract.yaml`：上述任务契约的机器镜像；用于后续 direct-dev 或受控任务包读取能力批次、后置边界和规则化准入标准
- `docs/自动开发任务包模板.md`：任务包模板，不是状态源

### 1.4 历史过程材料

- `docs/quality/*`
- `archive/*`
- `control/external_unlock_prerequisite_state.yaml` / `control/future_unlock_decision_state.yaml`：R6 决策时点快照，不是当前 repo readiness 状态源

## 2. 正式文档状态

| 资产 | 角色 | 当前状态 | 备注 |
|---|---|---|---|
| D1 | 执行手册 | DRAFT | 正式执行纪律与 change gate 来源之一 |
| D2 | 正式对象契约与字段字典 | DRAFT | |
| D3 | 正式规则码总表与双闸门说明 | DRAFT | |
| D4 | OpenAPI 接口契约 | DRAFT | |
| D5 | 页面、导出与人工复核规范 | DRAFT | |
| D6 | 字段策略与客户交付字段规范 | DRAFT | |
| D7 | 对象级交付矩阵与外发治理规范 | DRAFT | |
| D8 | 真实竞争者识别、可售对象与销售推进规范 | DRAFT | |
| D9 | 联系对象与销售触达规范 | DRAFT | |
| D10 | 订单支付交付与治理反馈规范 | DRAFT | |
| D11 | 测试验收与金标回归清单 | DRAFT | 正式回归 / release gate 来源之一 |
| D12 | 部署、发布与运行治理规范 | DRAFT | |
| D13 | 能力总表与放行边界总表 | DRAFT | 能力消费 / 放行 / 对外表达统一裁决来源 |
| D14 | AI / 模型治理规范 | DRAFT | |

### 2.1 当前专题与任务契约状态

| 资产 | 角色 | 当前状态 | 备注 |
|---|---|---|---|
| `docs/业务方向_候选公示后证据包与投前预测双线契约.md` | 招投标业务方向契约 | ACTIVE_REFERENCE | 当前双线产品方向主口径：候选公示后证据包分析为核心商业主线，投前预测分析为辅助产品线 |
| `contracts/evaluation/business_direction_strategy_contract.json` | 招投标业务方向机器契约 | EFFECTIVE | 后续 run mode、测试和脚本路由应读取该机器契约；不得把 tender_file smoke 误当成核心商业入口 |
| `docs/文档方向同步与合并治理计划.md` | docs 同步治理计划 | ACTIVE_REFERENCE | 规定哪些文档需要同步、哪些能力重叠只引用不合并、哪些旧口径暂不全文重写 |
| `docs/专题_投前预测评审风控规则救济与履约结算经验库草案.md` | 能力草案 / 规则候选池 | ACTIVE_REFERENCE | 只作为经验库能力池和候选规则来源；进入正式 D2/D3/contracts 前必须补字段血缘、证据等级、双闸门和测试 |
| `docs/任务契约_Stage1-6文件分析闭环与经验库能力落地.md` | Stage1-6 防漂移任务契约 | P0_FIRST_CUT_IN_PROGRESS | 当前唯一人类可读任务契约；第 1-4 批已完成第一刀，第 5 批为当前下一步 |
| `control/stage16_file_analysis_task_contract.yaml` | 任务契约机器镜像 | P0_FIRST_CUT_IN_PROGRESS | 镜像任务契约的批次、能力矩阵、Stage4-5 规则化边界；不替代 `current_task.yaml` |
| `docs/专题_来源覆盖与采集路由规范.md` | Stage1-2 来源覆盖 / 采集路由专题 | ACTIVE_REFERENCE | 只负责来源、路由、状态机、早期线索和字段血缘输入，不定义 Stage4-5 风控规则 |

P0 文档方向同步说明：高频入口文档必须引用双线方向契约或机器契约；`POST_CANDIDATE_EVIDENCE_PACK` 是核心商业主线，`PRE_BID_PREDICTION` 是辅助产品线，`tender_file` smoke 不能被写成最终业务入口，广东只保留广州交易集团主源。候选后负责人核验新增 `ResponsiblePersonEarlyProbe v1` 口径：先从 `07` 详情页/小 PDF 低成本拿公司、负责人、证书、报价和排名；缺证书号先公司优先补证，再姓名枚举兜底，仍不匹配才定向解析 `08 投标文件公开`，不得默认全量深解析。多候选人和联合体必须按候选行形成候选组，负责人/证书不得跨行混配；联合体成员全部进入核验目标，同一候选组任一成员匹配即可解析该组身份线索。

公开注册信息匹配同步说明：Stage4/5 只能表述“候选公示或 `08 投标文件公开` 中的姓名、候选公司/联合体成员、证书号、角色字段是否与四库或地方注册人员公开记录匹配”，不得写“是不是本人”。`姓名 + 注册单位/候选公司` 匹配时身份线索先成立，证书号、注册类别、注册专业、有效期和状态作为增强核验字段；不匹配才进入注册信息不一致风险线索。四库/JZSC 只作为公开注册信息、历史项目和 `08` 声明业绩匹配来源之一，不能作为实时在建冲突唯一依据；在建/履约冲突必须结合地方公共资源、地方住建/行政审批、施工许可、合同备案、竣工验收、项目经理变更、处罚/投诉等地方或网络公开线索。

EvidenceReport v1 同步说明：广州 5 项目核验报告默认不下载、不解析 `08 投标文件公开`，只登记 08 是否存在、URL、附件清单和后续可解析目标；只有 07 与 Stage4 公开注册信息缺字段、不匹配或需要核对 08 声明业绩时才触发 `FLOW_08_TARGETED_PARSE_REQUIRED`。报告必须分为核验线索/证据、过程稳定性、优化建议三类；`ActiveConflictProbe v1` 第一版只生成地方公共资源、地方住建/行政审批、施工许可、合同备案、竣工验收、项目经理变更、处罚/投诉和可回放网络线索的待核验任务清单。

GuangdongLocalVerificationProbe v1 同步说明：广东核验不限定广州市，默认按广东省级源优先、广州当前城市源补强执行。省级源包括广东三库一平台、广东建设信息网招投标及合同履约监管系统、广东投资项目审批监管、广东住建处罚公示和信用广东；广州项目额外加入广州市住建局信用信息双公示。该探针默认只做任务和可达性诊断，入口可达不等于字段核验成功，不得输出在建/履约最终结论。

GuangdongLocalFieldQueryProbe v1 同步说明：该探针消费 `GuangdongLocalVerificationProbe v1` 的 72 条任务，按公司、负责人、证书号和项目名做字段级只读查询探针。默认不联网，显式开启后只保存有限 readback、状态码、关键词命中摘要和失败 taxonomy；广州住建局信用信息双公示已接入 `guangzhou_zfcj_xyxx_api_query_v1`，可回放官方列表和详情 API 字段；广东建设信息网招投标及合同履约监管系统已接入 `guangdong_gdcic_contract_performance_public_page_v1`，可读取公开履约评价页并标记合同系统 SSO 登录阻断；广东省投资项目在线审批监管平台已接入 `guangdong_tzxm_project_approval_publicity_api_v1`，可回放备案、核准、审批和节能审查公开接口字段；广东省住建厅行政处罚公示已接入 `guangdong_zfcxjst_penalty_publicity_page_v1`，可从公示公告列表和详情页回放处罚/监管决定线索；信用广东已接入 `guangdong_credit_gd_public_credit_query_v1`，按 `/gdcreditwebApi2//company/web/booleanQueryListByPageSimple` 行政处罚/行政许可公开列表接口做字段回放，旧接口 404 归类为 stale endpoint，精准 `jsonArgs` 查询 403 只进入 review/blocker taxonomy，不阻断列表回放能力。关键词命中或字段回放只作为公开源线索，未命中或阻断不能写成排除风险，也不能替代其他源专项 adapter。

`AnalysisStrategyPlan v1` 同步说明：下载和解析前必须先做产品线分流。候选公示后主线默认从近期 07 中标候选人公示入池并回溯全流程，用于控标、围标、串标、陪标指数、真实竞争者和真实买家线索；09/10 中标结果、11 合同、12 异常属于回溯支撑或历史/后期核验，不是默认入口。投前预测只适用于近期 02/03/04 且投标截止/开标未过的项目，标准销售窗口为截止/开标前 168 小时以上，72-168 小时只做限时快筛，少于 72 小时不作为正常投前预测销售；只有 02/03 且无 04 时属于预澄清半成品预测，后续出现澄清、答疑、补遗或补充文件必须重新预测；一旦出现 05 开标信息即转入开标后核验或候选后证据包路线。近期 07 项目通常没有 11/12，缺 11/12 不阻断当前证据包销售窗口。广州 01-12 流程样例 URL 只作为适配器验证，不是生产爬取目标。

## 3. 正式机器资产状态

| 资产包 | 当前状态 | 备注 |
|---|---|---|
| contracts core | EFFECTIVE | schemas / enums / rules / gates |
| governance / release | EFFECTIVE | public boundary / coverage / field policy / approval / release |
| testing | EFFECTIVE | assertions / golden / regression / release checklist |
| handoff | EFFECTIVE | stage handoff / integration matrix / examples / validation / dependency order |
| scripts | EFFECTIVE | validate / golden / governance / release / lint / doctor / check-automation-readiness / check-handoff-dependencies |
| api / ui | EFFECTIVE | Stage 7-9 internal preview / draft route + projection + repository surfaces 已通过 `src/api/main.py` / `src/api/deps.py` 接入 FastAPI transport；Stage 1-6 仍为 contract-ready / transport-not-wired |
| exceptions / sales | EFFECTIVE | D7 / D8 / D9 / D10 机器承接 |
| future unlock prerequisites | EFFECTIVE | R5 machine-readable prerequisite baseline；不等于 external unlock |
| future unlock decision | EFFECTIVE | R6 decision matrix / state；只做 strategic classification，不做 implementation approval |
| leadpack activation design prep | DRAFT / REVIEW_READY | design / implementation prep 前置设计资产；review gate / approval / audit / owner signoff 已分层；不等于 implementation approved |
| leadpack implementation decision readiness packet | DRAFT / HELD | review-only signoff/readiness packet；不等于 implementation decision |
| control | CONFIRMED | 当前 state/source authority 已收敛到单一 canonical readiness；当前 active packet 为 `PKT-P0-08-S6-control-foundation-full-fix`，只在白名单 control 范围内执行 4 个控制层根因修复；AX9S full-repair 程序继续记为 `FULL_REPAIR_COMPLETE_REVIEW_READY`（仅程序控制态） |
| archive | EFFECTIVE | 仅历史追溯，不是现行引用面 |

补充一致性：
- `docs_layer=EFFECTIVE` 指“现行正式文档包生效”，不代表 D1-D14 每份文档 individually promoted to `EFFECTIVE`
- C 组枚举冻结：`CONFIRMED`

## 4. 当前正式状态

- 当前阶段：`PHASE_5_INTERNAL_LEADOPS_DEVELOPMENT`
- 当前判断：`READY_FOR_POST-REPAIR_MAINLINE_SELECTION`
- 当前 conditional-go：`READY_FOR_INTERNAL_LEADOPS_DEVELOPMENT`
- 当前 AX9S full-repair 程序控制状态：`FULL_REPAIR_COMPLETE_REVIEW_READY`
- 当前 FF 主链结论：`FF-01~FF-18 主链完成`
- 当前 formal implementation mainline：`未选定`
- 当前导航状态：`CONTROLLED_ROUTE_MAP_RESTORED`
- 当前是否 candidate-gap：`否`
- 当前是否 strategic-branch：`否`
- 当前 closure review：`已关闭`
- 当前 mainline selection：`就绪`
- 主要原因：当前 active packet 为 `PKT-P0-08-S6-control-foundation-full-fix`，只在白名单 control 范围内执行 4 个控制层根因修复；当前仓库维持主线选择前置就绪，但本窗不进入正式业务包。

作用域说明：
- `READY_FOR_POST-REPAIR_MAINLINE_SELECTION` 表示“当前仓库已完成 post-repair authority convergence，可以进入 formal mainline selection”，不表示 external 已放开，也不表示已经选定下一实现主线。
- `FULL_REPAIR_COMPLETE_REVIEW_READY` 只表示 AX9S full-repair 程序控制面已完成并可供复核；它不是 repo readiness source，也不改写当前 `READY_FOR_POST-REPAIR_MAINLINE_SELECTION`。
- `READY_FOR_INTERNAL_LEADOPS_DEVELOPMENT` 只用于内部 LeadOps 正式开发 conditional-go。
- `FF-18-S1` 只负责历史状态源与导航源最终单源收口，不新造新的业务 readiness token。
- `PKT-P0-08-S6-control-foundation-full-fix` 是当前 active control packet；它只修控制层根因，不自动进入正式业务包。
- `READY_FOR_POST-R6_CANDIDATE_GAP_BATCH` 与 `READY_FOR_POST-R6_STRATEGIC_BRANCH_BATCH` 只保留为历史 R6-path 语义，不再充当当前仓库总体 readiness。
- `docs/AX9S_开发执行路由图.md` 继续只是 navigation asset；在 machine-readable 索引中仍按 candidate navigation asset 标记，不是正式状态源。
- 本状态板与 `repo_status/current_task/milestone_status/release_manifest/model_release_manifest` 必须保持同一 phase/readiness。

## 5. 当前 blocked / governed 边界

- external software release：`CONTROLLED_OPENING`，需要 controlled-opening gate、release checklist、审批链、审计链、operator action、回滚/停机路径和回归通过后才能 live
- 线索包外发：继续要求审批链 + 审计链
- Stage 8 real execution：governed / approval-gated / controlled opening required
- Stage 9 real payment / delivery / refund：governed / approval-gated / controlled opening required
- model/provider/tool/source 真实外部接入：继续受 capability + review gate + governance 约束
- 高限制字段：继续按更严格规则处理
- future unlock implementation：必须基于 R6 decision + prerequisite evidence pack 另起单独 batch，不得由当前 R5/R6 资产或当前治理包自动触发

## 5.1 R6 decision 结果摘要

- FUTURE_UNLOCK_CANDIDATE：`leadpack_external_delivery`、`source_vendor_externalized_usage`、`external_export_surface`
- DENY_BY_DEFAULT_CONTINUE：`external_software_release`、`stage8_live_execution`、`stage9_live_payment_execution`、`stage9_live_delivery_execution`、`model_provider_externalized_usage`
- LONG_TERM_BLOCKED_OR_NEVER_DEFAULT_OPEN：`tool_provider_externalized_usage`
- 默认 post-R6 路径：`continue internal-first`

## 6. 使用规则

- 本状态板只允许存在一份正式版
- 更新时必须同步 `control/repo_status.md`、`control/current_task.yaml`、`control/milestone_status.yaml`、`control/release_manifest.yaml`、`control/model_release_manifest.yaml`、`control/reference_index.json`
- 不得用聊天、注释、口头说明替代本状态板

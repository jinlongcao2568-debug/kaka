结论：需修正任务包

判断：
- active subpacket 基本信息：当前 active 包是 `packet_id=subpacket_id=PKT-P0-08-S6-control-foundation-full-fix`，`source_blueprint_batch_id=PKT-P0-08-formal-route-map-split`，`packet_kind=EXECUTABLE_SCOPED_SUBPACKET`，`status=ACTIVE`，`depends_on=PKT-P0-08-formal-route-map-split`，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:109>)。它不是凭空自创；`source_blueprint_batch_id` 能在已登记 blueprint 中找到，且库规则明确允许“把 blueprint 拷贝进 `currentTask.task_packet` 激活”，见 [control/task_packet_library.yaml](</D:/Base One/kaka/control/task_packet_library.yaml:156>)、[control/task_packet_library.yaml](</D:/Base One/kaka/control/task_packet_library.yaml:160>)。`depends_on` 从状态面看可推定已满足，因为当前 `repo_status` 已把该治理包列为 current workstream，`milestone_status` 也已到 `READY_FOR_POST-REPAIR_MAINLINE_SELECTION`；但我没看到单独的 dependency resolved 字段，所以这里是基于状态的推定，不是显式完成标记，见 [control/repo_status.md](</D:/Base One/kaka/control/repo_status.md:4>)、[control/repo_status.md](</D:/Base One/kaka/control/repo_status.md:6>)、[control/milestone_status.yaml](</D:/Base One/kaka/control/milestone_status.yaml:12>)。
- 合规点：它符合“当前 active 来自 `current_task`、来源受 `task_packet_library` blueprint 约束、路线图只导航不当状态源”的方法框架，见 [AGENTS.md](</D:/Base One/kaka/AGENTS.md:88>)、[docs/AX9S_开发执行路由图.md](</D:/Base One/kaka/docs/AX9S_开发执行路由图.md:3>)。`docs` 变更 2 个，`control` 变更 3 个，分别没有超过 `max_docs: 5`、`max_control: 3`；`change_class=MANDATORY_HUMAN_REVIEW` 与其触达的高风险域匹配，`human_review_required=true`、`owner_reviews_required`、`signoff_required=true` 这些声明也一致，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:138>)、[control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:240>)、[control/automation_task_packet_rules.yaml](</D:/Base One/kaka/control/automation_task_packet_rules.yaml:15>)、[docs/自动化开发动作门禁表.md](</D:/Base One/kaka/docs/自动化开发动作门禁表.md:22>)、[docs/D1_研发_Codex执行手册.md](</D:/Base One/kaka/docs/D1_研发_Codex执行手册.md:833>)。`repo_status`、`milestone_status`、开工裁决页都允许当前处于“内部 LeadOps 开发 + post-repair mainline selection”状态继续这个治理包，但不代表自动获批进入正式业务实现主线，见 [control/repo_status.md](</D:/Base One/kaka/control/repo_status.md:4>)、[control/repo_status.md](</D:/Base One/kaka/control/repo_status.md:46>)、[control/milestone_status.yaml](</D:/Base One/kaka/control/milestone_status.yaml:49>)、[docs/正式业务代码开发开工裁决页.md](</D:/Base One/kaka/docs/正式业务代码开发开工裁决页.md:9>)、[docs/正式业务代码开发开工裁决页.md](</D:/Base One/kaka/docs/正式业务代码开发开工裁决页.md:90>)。
- 风险点：核心不合规点是 `declared_changed_paths` 有 11 个，超过任务包规则 `max_files: 10`，因此不满足“小包”要求；主题上仍算单主题控制修，但体量已经超了，按规则应先拆或收缩后再执行，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:152>)、[control/automation_task_packet_rules.yaml](</D:/Base One/kaka/control/automation_task_packet_rules.yaml:15>)、[control/automation_task_packet_rules.yaml](</D:/Base One/kaka/control/automation_task_packet_rules.yaml:90>)。第二个风险是 `signoff_status=REQUESTED_NOT_APPROVED`，这说明声明层面合规，但审批状态仍未到可直接执行实现的程度；`MANDATORY_HUMAN_REVIEW` 不能当 direct batch 继续跑，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:251>)、[docs/自动化开发动作门禁表.md](</D:/Base One/kaka/docs/自动化开发动作门禁表.md:60>)。第三个风险是 `required_scripts` 里 `check-automation-readiness.ps1` 重复了一次，不是缺项，但列表卫生不够好。
- 是否越界：路径边界本身没有越界。`declared_changed_paths` 全部都落在 `allowed_modification_paths` 内，且未命中 `forbidden_modification_paths`，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:152>)、[control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:166>)、[control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:178>)。它会触及 `tests/**`、`scripts/**`，也会触及 `contracts/testing/*` 这种高风险治理/发布面；但不触及 `src/**`、`contracts/governance/**`、`contracts/release/**`。
- 是否漏关键脚本：不漏。当前包已包含你特别关心的 `check-automation-readiness.ps1` 和 `check-release.ps1`，也带了 `doctor.ps1`、`check-handoff-dependencies.ps1`、`validate-contracts.ps1`、`run-golden.ps1`、`run-governance-contracts.ps1`、`lint-drift.ps1` 和测试入口，见 [control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:210>)。但本步我没有执行脚本；只读引用了 [control/repo_status.md](</D:/Base One/kaka/control/repo_status.md:57>) 里最近一次 PASS 记录。
- 是否符合“路线图指路、task_packet_library 给候选、current_task 唯一 active、scoped subpacket 单次执行、repo_status/milestone_status 看状态”这套方法：部分符合，不能直接进入实现。方法论主干是对的：`current_task` 是唯一 active，`task_packet_library` 提供 blueprint，`AX9S` 只导航，`repo_status/milestone_status` 才是状态真源，见 [AGENTS.md](</D:/Base One/kaka/AGENTS.md:88>)、[docs/AX9S_开发执行路由图.md](</D:/Base One/kaka/docs/AX9S_开发执行路由图.md:6>)、[README.md](</D:/Base One/kaka/README.md:22>)。但它没有满足 scoped subpacket 的 `max_files <= 10`，且当前审批状态仍是 `REQUESTED_NOT_APPROVED`，所以按长期稳定执行方法，当前应停在“修任务包 + 人工签审”，不是直接进实现。
- 当前如果进入实现，必须遵守的边界：先说结论，现状不应进入实现。若总控坚持推进，至少也必须先把 active 包收缩到规则上限内、保留同一主题、拿到对应 human review/signoff，再严格限制在白名单路径内，不能改 `docs/AX9S_开发执行路由图.md`、`docs/L0.md`、`docs/裁决总表.md`、`docs/D1_研发_Codex执行手册.md`、`control/task_packet_library.yaml`、`control/milestone_status.yaml` 等禁改面；不能改变 readiness/红线/外部放行语义；跑完 required scripts 后必须停机汇报，不能自动进入下一包，见 [control/task_packet_library.yaml](</D:/Base One/kaka/control/task_packet_library.yaml:164>)、[control/current_task.yaml](</D:/Base One/kaka/control/current_task.yaml:259>)、[AGENTS.md](</D:/Base One/kaka/AGENTS.md:68>)、[AGENTS.md](</D:/Base One/kaka/AGENTS.md:97>)。

给总控窗口的建议：
- 下一步应做什么：先不要进入实现。先把 `control/current_task.yaml` 里的 active subpacket 修到符合任务包规则，再走人工签审；修完后仍只允许执行这个治理包，执行完成后必须停机汇报。
- 如果不能继续，最小修正路径是什么：保持 `source_blueprint_batch_id=PKT-P0-08-formal-route-map-split` 不变，不自创新包名；把当前 active subpacket 的 `declared_changed_paths/allowed_modification_paths` 收缩到 `<=10`，保证仍是单主题控制修；顺手去掉 `required_scripts` 里的重复项；随后由总控补齐该包的人工签审状态，再决定是否允许进入实现。